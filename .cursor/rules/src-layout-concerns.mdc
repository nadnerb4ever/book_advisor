---
description: Keep code under src/ and split concerns into directories (nesting allowed)
alwaysApply: true
---

# Source layout and concerns

- **Application and library code** lives under **`src/`**, not at the repository root.
- **Separate concerns live in separate directories** (e.g. distinct packages or subpackages for CLI, domain logic, adapters, persistence—not one flat mix of unrelated modules).
- **Nesting is allowed:** a concern directory may contain subdirectories recursively whenever you need to break a concern into smaller concerns.
